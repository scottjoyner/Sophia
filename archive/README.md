# Archive

Orphan / dead / duplicate files moved here during the unified-fleet remediation
(LLD §3.1 W-12). They are preserved for reference but are NOT part of the
importable package and are excluded from packaging/tests.

- `Sophia_io.py` — legacy DeepSpeech dictation entrypoint (superseded by voice-agent/).
- `selenium.py` — abandoned UI scraper experiment.
- `start.sh` / `venv.sh` — ad-hoc shell helpers; replaced by docker-compose/CLI.
- `.DS_Store` — macOS metadata.
- `app_debug_js.js` — debug console script, not shipped.
- `setup.py` — empty; packaging moved to `voice-agent/pyproject.toml`.
